specVersion: "v1"
serviceId: "plansmart"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "plansmart-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
